export default class Equipo {

  // pais :: String, escudo :: Image?
  constructor(pais, escudo){
    this.pais = pais;
    // Estoy pensando en poner la imagen del escudo como atributo del objeto, no se si es muy viable eso
  }
    
}